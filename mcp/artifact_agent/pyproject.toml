[project]
name = "artifact-mcp"
version = "0.1.0"
description = "MCP server that exposes S3 artifacts as textual previews."
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.40.40",
    "mcp[cli]>=1.15.0",
    "openai>=2.2.0",
    "pillow>=11.3.0",
    "pydantic>=2.6.0",
]

