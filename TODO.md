## Today todo

### UI
1. All step tool calls should be renderd, not 1 message only and gets overwritten.
2. Add the rendering of CEL.md so user can see the thought process.
3. Allow chat box to expand when typing long text.
4. TODO logic: when the user does not enter /approve, the user should not be asked to type again, instead the logic should be instantly sent to the backend for an update after user feedback.

### MCP
1. Test full flow with the new NarrativeAgent.
2. Add meta data to the code artifacts (Generated by the code_agent) -> This will used to identify which piece of code generated which artifact.

### Backend


### NEW
1. SlideCraftAgent to the workflow: test with postman first. 